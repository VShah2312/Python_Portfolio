"""
File Handling: 


open -> close

3 modes to open (rt,rb, wt, wb, at, ab)

1. read ("r", default is "rt", for binary you "rb", binary to open any files other than txt files.), 
2. write ("w", default is "wt", when you try to do "wb" it will overwrite and it will turn blank),
3. append ("a", default is "at",append binary doesnt work only memory keeps increasing.)


t-> text
b-> binary
"""
