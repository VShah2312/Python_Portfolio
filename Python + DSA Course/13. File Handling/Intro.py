"""
File Handling: 


open -> close

6 modes to open

t-> text
read ("r", default is "rt", for binary you "rb", binary to open any files other than txt files.), 
write ("w", when you try to do "wb" it will overwrite and it will turn blank),
 append ("a", append binary doesnt work only memory keeps increasing.)

b-> binary
"""
